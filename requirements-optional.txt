# ──────────────────────────────────────────────────────────────────────
# Quant Trading System — Optional ML / RL / NLP dependencies
# ──────────────────────────────────────────────────────────────────────
# Install:  pip install -r requirements-optional.txt
#
# These are lazy-imported in the codebase. The app runs without them,
# but specific features (boosting models, RL training, NLP sentiment,
# plotting, SHAP explainability) require them.
# ──────────────────────────────────────────────────────────────────────

# ML / boosting
lightgbm>=4.3
xgboost>=2.0

# Plotting & explainability
matplotlib>=3.8
shap>=0.45

# Reinforcement learning
gymnasium>=0.29
stable-baselines3>=2.3

# NLP / transformers for sentiment analysis
transformers>=4.40

# Dev / testing
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100
ruff>=0.6
mypy>=1.11
