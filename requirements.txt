# ──────────────────────────────────────────────────────────────────────
# Quant Trading System — Python dependencies (OS-agnostic core)
# ──────────────────────────────────────────────────────────────────────
# Install:  pip install -r requirements.txt
#
# For CUDA-accelerated torch, install torch separately BEFORE this file:
#   Linux:   pip install torch --index-url https://download.pytorch.org/whl/cu121
#   Windows: pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# For CPU-only torch (no CUDA):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# ──────────────────────────────────────────────────────────────────────

# Database
psycopg2-binary>=2.9
sqlalchemy>=2.0
alembic>=1.13

# Data
pandas>=2.2
numpy>=1.26
yfinance>=0.2

# Computation
scipy>=1.13
scikit-learn>=1.5
statsmodels>=0.14

# Astronomy (market session sunrise/sunset)
ephem>=4.1

# HMM regime detection
hmmlearn>=0.3

# GPU (install torch separately — see header comment)
# torch>=2.2

# Web
fastapi>=0.115
uvicorn>=0.30

# HTTP / scraping
requests>=2.32
cloudscraper>=1.2
curl_cffi>=0.7

# Utils
python-dotenv>=1.0
structlog>=24.1
